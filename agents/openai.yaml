interface:
  display_name: "Codex Bundled Plugins"
  short_description: "Restore bundled plugins and browser helpers"
  default_prompt: "Use $codex-bundled-plugins to resync Codex desktop bundled plugins and repair Browser Use or Chrome helper binaries on Windows."

policy:
  allow_implicit_invocation: false
